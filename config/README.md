# Configuração

| Caminho | Conteúdo |
|---------|----------|
| `config/icps/*.yaml` | Perfis de cliente ideal (ICP) por nicho |
| `config.py` (raiz) | Variáveis de ambiente, `SiteData`, `OUTPUT_DIR` |

ICPs editáveis: keywords, locais, score, oferta sugerida e template de protótipo recomendado.
